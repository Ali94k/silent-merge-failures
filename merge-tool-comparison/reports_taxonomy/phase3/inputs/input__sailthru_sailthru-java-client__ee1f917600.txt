UNIT sailthru_sailthru-java-client__ee1f917600
repo: sailthru_sailthru-java-client   merge: ee1f9176009eddb98d87251dfd9d8476b8684fc3   stratum: A
files shown: 1 of 1 intersecting Java files
dropped (textual-conflict, not silent — excluded): (none)
truncation level: T0  (full inputs)

=== FILE 1/1: src/main/com/sailthru/client/exceptions/ApiException.java ===
--- OURS vs BASE (unified diff, -U8) ---
--- base
+++ ours
@@ -1,33 +1,35 @@
 package com.sailthru.client.exceptions;
 
 import java.io.DataInputStream;
 import java.io.EOFException;
 import java.io.IOException;
 import java.io.InputStream;
 import java.util.HashMap;
 import java.util.Map;
-import java.util.logging.Logger;
 import org.apache.http.StatusLine;
+import org.slf4j.Logger;
+import org.slf4j.LoggerFactory;
+
 
 /**
  * Handle API related Exceptions
  * @author Prajwal Tuladhar <praj@sailthru.com>
  */
 public class ApiException extends IOException {
 
-    private static Logger logger = Logger.getLogger(ApiException.class.getName());
+    private static Logger logger = LoggerFactory.getLogger(ApiException.class);
 
     private Map<String, Object> jsonResponse;
     private int statusCode;
 
     public ApiException(int statusCode, String reason, Object jsonResponse) {
         super(reason);
-        logger.info(String.format("%d: %s", statusCode, reason));
+        logger.info("{}: {}", statusCode, reason);
         this.jsonResponse = (Map<String, Object>)jsonResponse;
         this.statusCode = statusCode;
     }
 
     public Map<String, Object> getResponse() {
         return jsonResponse;
     }
 
--- THEIRS vs BASE (unified diff, -U8) ---
--- base
+++ theirs
@@ -1,33 +1,29 @@
 package com.sailthru.client.exceptions;
 
-import java.io.DataInputStream;
-import java.io.EOFException;
 import java.io.IOException;
-import java.io.InputStream;
-import java.util.HashMap;
 import java.util.Map;
 import java.util.logging.Logger;
 import org.apache.http.StatusLine;
 
 /**
  * Handle API related Exceptions
  * @author Prajwal Tuladhar <praj@sailthru.com>
  */
 public class ApiException extends IOException {
 
-    private static Logger logger = Logger.getLogger(ApiException.class.getName());
+    private static final Logger logger = Logger.getLogger(ApiException.class.getName());
 
     private Map<String, Object> jsonResponse;
     private int statusCode;
 
     public ApiException(int statusCode, String reason, Object jsonResponse) {
         super(reason);
-        logger.info(String.format("%d: %s", statusCode, reason));
+        logger.warning(String.format("%d: %s", statusCode, reason));
         this.jsonResponse = (Map<String, Object>)jsonResponse;
         this.statusCode = statusCode;
     }
 
     public Map<String, Object> getResponse() {
         return jsonResponse;
     }
 
--- MERGED RESULT (mergiraf; full) ---
package com.sailthru.client.exceptions;

import java.io.IOException;
import java.util.Map;
import org.apache.http.StatusLine;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;


/**
 * Handle API related Exceptions
 * @author Prajwal Tuladhar <praj@sailthru.com>
 */
public class ApiException extends IOException {

    private static final Logger logger = LoggerFactory.getLogger(ApiException.class);

    private Map<String, Object> jsonResponse;
    private int statusCode;

    public ApiException(int statusCode, String reason, Object jsonResponse) {
        super(reason);
        logger.warning("{}: {}", statusCode, reason);
        this.jsonResponse = (Map<String, Object>)jsonResponse;
        this.statusCode = statusCode;
    }

    public Map<String, Object> getResponse() {
        return jsonResponse;
    }

    public int getStatusCode() {
        return statusCode;
    }

    public static ApiException create(StatusLine statusLine, Object jsonResponse) {
        int statusCode = statusLine.getStatusCode();
        Map<String, Object> response = (Map<String, Object>)jsonResponse;
        return new ApiException(statusCode, response.get("errormsg").toString(), jsonResponse);
    }
}
--- MERGED vs DEVELOPER RESOLUTION (unified diff, -U8) ---
--- merged
+++ developer
@@ -15,17 +15,17 @@
 
     private static final Logger logger = LoggerFactory.getLogger(ApiException.class);
 
     private Map<String, Object> jsonResponse;
     private int statusCode;
 
     public ApiException(int statusCode, String reason, Object jsonResponse) {
         super(reason);
-        logger.warning("{}: {}", statusCode, reason);
+        logger.warn("{}: {}", statusCode, reason);r
         this.jsonResponse = (Map<String, Object>)jsonResponse;
         this.statusCode = statusCode;
     }
 
     public Map<String, Object> getResponse() {
         return jsonResponse;
     }
 
--- COMPILE DELTA (javac, single-file, eclipse-temurin:17-jdk; PARSE/RESOLVE/OTHER) ---
[RESOLVE] package org.apache.http does not exist
[RESOLVE] package org.slf4j does not exist
[RESOLVE] package org.slf4j does not exist
[RESOLVE] cannot find symbol
[RESOLVE] cannot find symbol
[RESOLVE] cannot find symbol

<!-- {UNIT_INPUTS} = per file, in this order:
  === FILE k/n: <path> ===
  --- OURS vs BASE (unified diff, -U8) ---
  --- THEIRS vs BASE (unified diff, -U8) ---
  --- MERGED RESULT (mergiraf; full if ≤700 lines, else windowed ±40 around
      regions differing from base, plus package/imports/enclosing signatures) ---
  --- MERGED vs DEVELOPER RESOLUTION (unified diff, -U8) ---
  --- COMPILE DELTA (javac, single-file, eclipse-temurin:17-jdk):
      errors on MERGED absent on DEV, classified PARSE/RESOLVE/OTHER;
      "(none)" when empty ---
-->